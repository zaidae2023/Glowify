<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.glowify">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Glowify"
        tools:targetApi="31">

        <!-- MainActivity -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".NextActivity"/>

        <!-- DullSkinActivity -->
        <activity android:name=".DullSkinActivity"/>

        <!-- DrySkinActivity -->
        <activity android:name=".DrySkinActivity"/>

        <!-- OilySkinActivity -->

        <activity android:name=".OilySkinActivity"/>

        <!-- SensitiveSkinActivity -->

        <activity android:name=".SensitiveSkinActivity"/>

        <!-- CombinationSkinActivity -->

        <activity android:name=".CombinationSkinActivity"/>

        <!-- AcneProneSkinActivity -->

        <activity android:name=".AcneProneSkinActivity"/>

        <!-- NormalSkinActivity -->

        <activity android:name=".NormalSkinActivity"/>

        <!-- SunDamagedSkinActivity -->

        <activity android:name=".SunDamagedSkinActivity"/>






    </application>

</manifest>
